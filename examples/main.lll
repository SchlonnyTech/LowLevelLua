import greetmodule;

function main(): int
    print("Hello, LLL!")
    print("This is a real compiled language now!")

    local name: string = "LLL"
    print("Welcome to " .. name)

    local greeting: string = greetmodule.greet("LLL")
    print(greeting)

    return 0
end
